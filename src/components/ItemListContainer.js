
import Duracion from "./Duracion";

const ItemListContainer = ({valor, valor2}) => {
    return (
        <div> ItemListContainer {valor}, {valor2}
            <Duracion></Duracion>
        </div>
    )
}

export default ItemListContainer;

{/* <button onClick= {() => (confirmacionEnviado)}>Comprar</button> */}